<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>SGS Jainism Park Expo 26</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <meta charset="utf-8" />
    <title>SGS Jainism Park Expo 26</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Font Awesome for WhatsApp icon -->
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <!-- COMMON BUTTON STYLE -->
    <style>
        .btn-red {
            background-color: #dc2626; /* RED */
            color: #ffffff; /* WHITE TEXT */
            border: none;
            border-radius: 14px;
            padding: 16px 20px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            width: 100%;
            text-decoration: none;
        }

         .btn-red:hover {
             background-color: #b91c1c;
         }
        /* GAP FIXES */
        .btn-gap-top {
            margin-top: 18px;
        }

        .btn-gap-bottom {
            margin-bottom: 20px;
        }

        /* BUTTON SIZE NORMALIZATION */
        .btn-same-size {
            padding: 14px 16px;
            font-size: 16px;
            width: 100%;
            box-sizing: border-box;
        }

        @@media (max-width: 768px) {
            .btn-red

        {
            font-size: 15px;
            padding: 14px 16px;
        }

        }
    </style>
    <!-- ✅ CSS MUST BE INSIDE STYLE -->
    <style>

        /* HEADER BASE */
        .global-header {
            position: relative;
            background: #020079;
            color: white;
            padding: 20px 16px 18px;
            text-align: center;
        }

        /* LOGO */
        .expo-logo {
            position: absolute;
            top: 18px;
            left: 100px;
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            @*background: #ffffff;*@
            box-shadow: 0 6px 16px rgba(0,0,0,0.25);
            background: none !important; /* 🔥 remove white background */
            padding: 0 !important;
            object-fit: cover;
        }

        /* TITLE BESIDE LOGO (DESKTOP DEFAULT) */
        .header-title-block {
            margin-left: 0;
            padding-top: 12px;
        }

        .header-title-main {
            font-size: 32px;
            text-align: center;
            font-weight: 800;
            line-height: 1.2;
        }

        .header-title-sub {
            text-align: center;
            font-size: 24px;
            font-weight: 600;
            margin-top: 2px;
        }

        /* HEADER TEXT (DESKTOP: SINGLE FLOW) */
        .header-bottom-text {
            margin-top: 16px;
            text-align: center;
        }

        .header-location {
            font-size: 13px;
            line-height: 1.4;
        }

        .header-note {
            font-size: 14px;
            font-weight: 500;
            margin-top: 10px; /* ✅ REQUIRED 5px GAP */
        }

        /* ================= MOBILE ONLY ================= */
        @@media (max-width: 768px) {
            .expo-logo

        {
            left: 40px;
            width: 70px;
            height: 70px;
            top: 14px;
            background: none !important; /* 🔥 remove white background */
            padding: 0 !important;
            object-fit: cover;
        }

        .header-title-block {
            margin-left: 120px;
            padding-top: 0;
        }

        .header-title-main {
            text-align: center;
            font-size: 24px;
        }

        .header-title-sub {
                text-align: center;
                font-size: 20px;
        }

        /* ✅ MOBILE: STACK LOCATION + BOOK LINE */
        .header-bottom-text {
            margin-top: 14px;
            text-align: center;
            display: block;
        }

        .header-location {
            font-size: 12px;
            display: block;
        }

        .header-note {
            font-size: 13px;
            display: block;
            margin-top: 10px; /* ✅ 5px SPACE */
        }

        }
    </style>
</head>

<body style="margin:0; font-family:system-ui, -apple-system, Segoe UI, Roboto;">

    <!-- GLOBAL HEADER -->
    <div class="global-header">

        <!-- Logo -->
        <img src="/images/logo-sgs-final.png"
             alt="SGS Jainism Park Expo Logo"
             class="expo-logo" />

        <!-- Title beside logo -->
        <div class="header-title-block">
            <div class="header-title-main">SGS Jainism Park</div>
            <div class="header-title-sub">Expo 2026, Vadodara</div>
        </div>

        <!-- Header bottom text -->
        
        <div class="header-bottom-text">
            <div class="header-note">
                Book your Entry Slot to avoid waiting time on 1st Feb, 2026.
            </div>
            <div class="header-location">
                <strong>Location:</strong> Shree Champak Guru Aradhana Bhavan - 1,
                Arbudanagar Soc., Nr. Atmajyoti Ashram, Ellorapark, Vadodara.
            </div>

           
        </div>

    </div>



    <!-- PAGE CONTENT -->
    @RenderBody()

    <!-- FOOTER -->
    <div style="
        background:#0f172a;
        color:white;
        text-align:center;
        padding:22px;
        font-size:14px;
    ">
        🙏 Jai Jinendra 🙏<br />
        © 2026 SGS Jainism Park Expo 26. All rights reserved.
    </div>

</body>
</html>
