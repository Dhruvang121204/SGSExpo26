<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>SGS Jainism Park Expo 26</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- ✅ CSS MUST BE INSIDE STYLE -->
    <style>
        .global-header {
            position: relative;
            background: linear-gradient(to right,#f97316,#ef4444);
            color: white;
            padding: 50px 16px 56px;
            text-align: center;
        }

        .global-greeting {
            font-size: 16px;
            margin-bottom: 10px;
        }

        .global-title {
            font-size: 34px;
            font-weight: 800;
        }

        .global-subtitle {
            margin-top: 12px;
            font-size: 18px;
            max-width: 720px;
            margin-left: auto;
            margin-right: auto;
        }

        .expo-logo {
            position: absolute;
            left: 30px;
            top: 50%;
            transform: translateY(-50%);
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
            background: #ffffff;
            box-shadow: 0 8px 20px rgba(0,0,0,0.25);
        }

        /* ✅ MOBILE RESPONSIVE FIX */
        @@media (max-width: 768px) {

            .global-header {
                padding: 100px 16px 32px; /* extra top space for logo */
            }

            .expo-logo {
                width: 90px;
                height: 90px;
                /* move logo to top center */
                left: 20px;
                top: 20px;
                transform: none;
            }

            .global-greeting {
                position: absolute;
                top: 46px;
                left: 140px;
                font-size: 26px;
                margin: 0;
                white-space: nowrap;
            }

            .global-title {
                margin-top: 28px;
                font-size: 22px;
                line-height: 1.3;
            }

            .global-subtitle {
                font-size: 14px;
            }
        }

    </style>
</head>

<body style="margin:0; font-family:system-ui, -apple-system, Segoe UI, Roboto;">

    <!-- GLOBAL HEADER -->
    <div class="global-header">

        <!-- LOGO -->
        <img src="/images/sgs-expo-logo.png"
             alt="SGS Jainism Park Expo Logo"
             class="expo-logo" />

        <div class="global-greeting">
            🙏 Jai Jinendra 🙏
        </div>

        <div class="global-title">
            SGS JAINISM PARK, EXPO 2026, VADODARA
        </div>

        <div class="global-subtitle">
            Book your Entry Slot to avoid waiting time on 1st Feb, 2026
        </div>
    </div>

    <!-- PAGE CONTENT -->
    @RenderBody()

    <!-- FOOTER -->
    <div style="
        background:#0f172a;
        color:white;
        text-align:center;
        padding:22px;
        font-size:14px;
    ">
        🙏 Jai Jinendra 🙏<br />
        © 2026 SGS Jainism Park Expo 26. All rights reserved.
    </div>

</body>
</html>
